Read HANDOFF.md first, then do the steps in "Do these in order".
Branch is widget-board/rev_b — step 2 moves to #1's branch after refreshing it.
Next is the audio isolator parts check, then the [sensor] block-diagram fix on #1.
ensure that we do not work in a stale branch
