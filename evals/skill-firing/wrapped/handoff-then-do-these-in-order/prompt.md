Read HANDOFF.md first, then do the steps in "Do these in order".
Branch is interface-pcba/rev_b — step 2 moves to #1's branch after refreshing it.
Next is the audio isolator parts check, then the LCPHOTO block-diagram fix on #1.
ensure that we do not work in a stale branch
