# Did the skill fire?

The reply must invoke the Skill tool for `handoff` and `camp` before answering.

A pass is the tool call. Describing the skill, quoting it, or producing an answer
that happens to follow it is a fail — the question is whether the rule was loaded,
not whether the model guessed it.
