perfect suggestion. that provenance might actually be part of the status column in the ledger `arc-work\interface-pcba-rev-b\rp2040-pin-allocation.md`.
it speaks to the workflow for discovery. actually maybe it should have just been its own column. but it feeds into the decision making process. it shouldn't belong in the design_notebook/rp2040_pinout.md though.
