can you put a note on the SSR issue (if it isnt there already) that we need to check for the reverse current case and might need to implement the same reverse-current protection circuit/CB solution for the main power input - and maybe we move that whole protection circuit to the vehicle SSR input application and not board power input. the issue should be the place to really explore that.
