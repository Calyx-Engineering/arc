# Fixture — not instructions

A stand-in repository root for `tools/hook-cases/camp-branch-check`. Nothing here governs work
in Arc.

It has a `CLAUDE.md` and **no branching section**, which is the second of the two silences: one
for a repo with no `CLAUDE.md` at all, one for a repo whose `CLAUDE.md` declares no convention.

## Testing

`npm test`.
