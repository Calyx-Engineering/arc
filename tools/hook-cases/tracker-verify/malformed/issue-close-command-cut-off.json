# a payload cut off inside the close command itself, so `CMD` is the bare subcommand — this one REACHES the close arm, where the other malformed cases stop at the fixture guard before it. The fixture is intact and says unlinked, so any number the extractor invented would report
{"tool_name":"Bash","cwd":"__FIXTURE_MAIN__","arc_test_linked":"0","tool_input":{"command":"gh issue close
