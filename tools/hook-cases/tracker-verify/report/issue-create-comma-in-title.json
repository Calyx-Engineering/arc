# an issue created with a comma in its title — the comma must not change what this arm does. REGRESSION COVER, not a demonstration: the create arm's finding comes from the body fixture, which the truncated tail never held, so this case reported before the fix too. `issue-close-comma-in-comment` and `pr-edit-comma-in-label` are the ones that decide. #230
{"tool_name":"Bash","cwd":"__FIXTURE_MAIN__","arc_test_body":"## Required\n\n- [ ] set the current limit to TBD mA\n","tool_input":{"command":"gh issue create --title \"fix: one thing, and the cases for it\" --body-file body.md"}}
